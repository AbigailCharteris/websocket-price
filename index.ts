// "use strict";


// import * as angular from "angular";

// angular.module("RegistrationApp", ["ngRoute"])
// 	// The logger to demonstrate AngularJS dependency injection
// 	//.factory("logger", () => new DefaultLogger())
// 	// Our controllers for the two views
// 	//.controller("RegistrationsController", RegistrationsViewModel)
// 	// .controller("RegisterController", RegisterViewModel)
// 	// The routes for the SPA
// 	.config(($routeProvider: ng.route.IRouteProvider) => {
// 		$routeProvider
// 			.when("/", {
// 				template: "<div>Hello world</div>" 
// 			})
// 			// .when("/register", {
// 			// 	templateUrl: "register.html", controller: "RegisterController"
// 			// });
// });

// export default module;
// // "use strict";

// // import { AppConfig } from "./common/AppConfig";
// // import * as angular from "angular";
// //import appModule from "./AppModule";

// //  let module = angular.module("bakerTechApp", ["ui.router", "ui.bootstrap", "ct.ui.router.extras", appModule.name]);
// // let module = angular.module("App",[] );
// // export default module;

// // export interface Window {
// //     bootstrapApp(element: HTMLElement): void;
// // }

//             //angular.bootstrap(new Element, [appModule.name], {strictDi: true});


// // (<any>window).bootstrapApp = (element: HTMLElement) => {
// // //this.window.bootstrapApp = (element: HTMLElement) => {
     
// //     function start(element: HTMLElement) {
// //         angular.element().ready(() => {
// //             element.innerHTML = "<div class=\"gs-search-app-container\">" +
// //                 "<div ui-view></div>" +
// //                 "<div ui-view=\"modal\" class=\"modal-view\" ng-class=\"{'active' : ctrl.isActive}\"></div>" +
// //                 "</div>";

// //             angular.bootstrap(element, [appModule.name], {strictDi: true});
// //         });
// //     };
// // };